import styled from "styled-components";

export const TPTitle = styled.h1`
  font-weight: 700;
  font-size: 18px;
  line-height: 27px;

  margin-top: 36px;

  &:first-child {
    margin-top: 0px;
  }

  color: #111111;
`;

export const TPSubTitle = styled.div`
  font-weight: 700;
  font-size: 14px;
  line-height: 21px;

  margin: 24px 0 4px 0;

  color: #111111;
`;

export const TPContent = styled.div`
  font-weight: 400;
  font-size: 12px;
  line-height: 18px;

  color: #505050;
`;
